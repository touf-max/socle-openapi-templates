# Profil "events" — webhooks poussés vers mes partenaires ; opérations sous `webhooks:` par le build.
# Payload brut dans le requestBody, aucune enveloppe. Sécurité : bearer JWT héritée du core.
# Signature HMAC : reportée (cf. SPEC §12).
webhooks: {}
