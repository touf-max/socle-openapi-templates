# Squelette OpenAPI commun (couche 1). info/servers/tags sont fournis par le projet.
# Sécurité généralisée à TOUS les types : bearer JWT (jamais d'API key).
openapi: 3.1.0
info:
  title: Untitled API
  version: 0.0.0
components:
  schemas: {}
  parameters: {}
  headers: {}
  responses: {}
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
